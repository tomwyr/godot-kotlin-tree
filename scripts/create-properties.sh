cat > local.properties << EOFecho
        gradle.publish.key=
        gradle.publish.secret=
        maven.repo.username=
        maven.repo.password=
        maven.signing.key=
        maven.signing.password=
        EOF
